const viewScheduledCommand = (req, res, next) => {
  return res.status(200).json({
    output: {
      text: "View Scheduled Message Yet To Be Implemented..",
    },
  });
};

module.exports = viewScheduledCommand;
